// Template script that you can put into the blot interface to preview
// a drawing. Please use preprocessor.py and blot.js to generate the
// required output text.

const WIDTH = 125;
const HEIGHT = 125;

setDocDimensions(WIDTH, HEIGHT);

/*

    PASTE OUTPUT TEXT ON THE LINE BELOW (REPLACE THE BRACKETS)

*/

let finalLines = [];
drawLines(finalLines);
